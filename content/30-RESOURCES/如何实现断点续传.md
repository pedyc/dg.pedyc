---
uid: 202607071520
title: 如何实现断点续传
aliases: ["Q-断点续传", "断点续传与大文件上传"]
description: "前端大文件断点续传的核心原理与完整实现，包含文件切片、并发控制、服务端合并等环节"
tags: []
date-created: 2026-07-07
date-modified: 2026-08-19
status: fleeting
content-type: question
up: "[[前端场景实践]]"
---

> 收敛：[[SOP-实现大文件上传]]

## 问题

> 前端如何实现大文件断点续传？上传中途中断后如何恢复？

---

## 背景

浏览器上传大文件（如视频、安装包、数据集）时，网络波动或服务端超时可能导致上传中断，从头重传成本极高。断点续传的核心思路：将大文件切分成多个小块（chunk），结合哈希指纹实现**分片并发上传**、**断点续传**与**秒传**，中断后只重传失败的 chunk。

---

## 解决方案

**整体流程：**
1. 文件切片与唯一标识生成
	- 利用`Blob.prototype.slice`将文件拆分为指定大小的chunk（如 5MB ~ 10MB）。
	- 同时对整个文件计算唯一特征码（MD5/SHA-256）。
2. 秒传与续传预检
	- 向后端发送文件Hash、文件名、文件大小
	- 秒传：若后端存在该Hash的完整文件，直接返回成功，跳过上传
	- 续传：若只存在部分分片，服务端返回已上传成功的切片索引列表，前端滤过这一部分
3. 分片并发上传与并发控制
	- 将未上传的分片通过 FormData发送给服务端（携带hash、index、chunk等元数据）。使用并发控制队列并发发送，避免占满浏览器连接池
4. 全部完成后通知服务端合并
	- 当所有分片上传完成后，通知后端更具文件Hash及分片总数，将所有临时分片按顺序合并为最终文件，并清理临时切片

**前端核心代码：**

```js
// 1. 文件 hash（SHA-256）
async function getFileHash(file) {
  const buffer = await file.arrayBuffer()
  const hashBuffer = await crypto.subtle.digest('SHA-256', buffer)
  const hashArray = Array.from(new Uint8Array(hashBuffer))
  return hashArray.map(b => b.toString(16).padStart(2, '0')).join('')
}

// 2. 切片（默认 5MB）
function createChunks(file, chunkSize = 5 * 1024 * 1024) {
  const chunks = []
  let start = 0
  while (start < file.size) {
    chunks.push(file.slice(start, start + chunkSize))
    start += chunkSize
  }
  return chunks
}

// 3. 上传主流程
async function uploadFile(file) {
  const hash = await getFileHash(file)
  const chunks = createChunks(file)

  // 查询服务端已上传的 chunk
  const res = await fetch(`/api/status?hash=${hash}`)
  const { uploaded } = await res.json()  // Set<index>

  // 并发控制，逐批上传
  const pool = new Set()
  for (let i = 0; i < chunks.length; i++) {
    if (uploaded.has(i)) continue

    const form = new FormData()
    form.append('chunk', chunks[i])
    form.append('hash', hash)
    form.append('index', i)
    form.append('total', chunks.length)

    const task = fetch('/api/upload', { method: 'POST', body: form })
      .then(r => { if (!r.ok) throw new Error(`chunk ${i} 失败`) })
    pool.add(task)
    task.finally(() => pool.delete(task))
    if (pool.size >= 4) await Promise.race(pool) // 限流
  }
  await Promise.all(pool)

  // 通知合并
  await fetch('/api/merge', { method: 'POST', body: JSON.stringify({ hash, total: chunks.length }) })
}
```

**服务端关键逻辑：**

```js
// 接收 chunk → 存入 /tmp/{hash}/{index}
app.post('/api/upload', async (req, res) => {
  const { hash, index } = req.body
  await fs.mkdir(`/tmp/${hash}`, { recursive: true })
  await fs.writeFile(`/tmp/${hash}/${index}`, req.file.buffer)
  res.json({ ok: true })
})

// 查询已上传的 chunk index
app.get('/api/status', async (req, res) => {
  const { hash } = req.query
  const dir = `/tmp/${hash}`
  if (!fs.existsSync(dir)) return res.json({ uploaded: [] })
  const files = await fs.readdir(dir)
  res.json({ uploaded: files.map(Number) })
})

// 合并所有 chunk
app.post('/api/merge', async (req, res) => {
  const { hash, total } = req.body
  const dir = `/tmp/${hash}`
  const ws = fs.createWriteStream(`/uploads/${hash}`)
  for (let i = 0; i < total; i++) {
    ws.write(await fs.readFile(`${dir}/${i}`))
  }
  ws.end()
  await fs.rm(dir, { recursive: true })
  res.json({ url: `/uploads/${hash}` })
})
```

### 设计要点

| 问题 | 方案 |
|------|------|
| 文件唯一标识 | 浏览器 SHA-256 hash |
| 断点恢复 | 上传前调 `/api/status` 获取已上传 index |
| 并发控制 | 限制 4~6 并发，防浏览器连接上限 |
| 弱网恢复 | 单个 chunk 独立重试（指数退避） |
| 暂停/继续 | `AbortController` 控制请求 |

### 我的理解

断点续传的核心不在于"传"而在于"查"——每次上传前查一下服务端有哪些 chunk，传输过程其实是无状态的。hash 计算是另一个瓶颈，大文件用 Web Worker 跑 SHA-256 避免卡 UI。性能优化可以考虑抽样Hash，例如：取文件前2MB、中间2MB、后2MB，加上其他切片各截取前后的2KB进行组合计算Hash。

---

## 探索路径

- [x] 几GB大小的超大文件，计算MD5时会直接卡死，该如何优化？ ✅ 2026-08-19
	- Web Worker：将文件分片后，通过`postMessage`将分片数据传给Web Worker，在后台计算MD5
	- 抽样Hash：如果业务对准确度没有严苛要求，可以通过抽样Hash（文件头部2MB、尾部2MB、中间几个分片的部分数据生成一个特征Hash）大幅优化性能
- [x] 如何通过代码实现你提到的并发控制？HTTP/2 时代还需要并发控制吗？
	- 可以创建一个[[如何实现一个带并发限制的异步任务调度器（Scheduler）|并发限制调度器]]来实现
	- 虽然 HTTP/2 具有 多路复用 的特性，但前端还是需要进行并发控制，否则一次性向后端发送几十上百个分片可能导致服务端内存溢出，或客户端网卡过载
- [ ] 对比 Web Streams API 实现流式切片
- [ ] 调研 multipart upload 的云服务方案（OSS/S3）
- [ ] Web Worker 中跑 hash 计算的性能测试

---

## 待验证（扩展）

- [ ] 用 `navigator.onLine` 事件实现网络恢复后自动续传
- [ ] 多文件上传队列的调度策略
- [ ] 服务端 chunk 临时目录的垃圾回收策略

---

## 关联

- **相关问题**：[[如何实现一个带并发限制的异步任务调度器（Scheduler）]]
- **参考资料**：MDN File API, crypto.subtle.digest
