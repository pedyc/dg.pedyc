---
uid: 202607281003
title: 装饰器模式
aliases: [T-装饰器模式, Decorator Pattern]
description: 一种结构型设计模式，动态地给对象添加额外的职责，而不改变其原始类
tags: ["软件工程/设计模式"]
date-created: 2026-07-28
date-modified: 2026-07-28
status: fleeting
content-type: term
up: ["[[设计模式]]"]
---

## 术语：装饰器模式

> **主题**：#软件工程/设计模式

### 定义

**装饰器模式**（Decorator Pattern）是一种结构型设计模式，通过将对象包装在装饰器对象中，动态地为其添加新行为。它提供了一种比继承更灵活的扩展方式——继承是编译时静态扩展，装饰器是运行时动态组合。

```typescript
interface Coffee {
  cost(): number;
  description(): string;
}

class SimpleCoffee implements Coffee {
  cost() { return 10; }
  description() { return 'Coffee'; }
}

class MilkDecorator implements Coffee {
  constructor(private coffee: Coffee) {}
  cost() { return this.coffee.cost() + 3; }
  description() { return `${this.coffee.description()}, Milk`; }
}

class SugarDecorator implements Coffee {
  constructor(private coffee: Coffee) {}
  cost() { return this.coffee.cost() + 1; }
  description() { return `${this.coffee.description()}, Sugar`; }
}

// Usage
let coffee: Coffee = new SimpleCoffee();
coffee = new MilkDecorator(coffee);
coffee = new SugarDecorator(coffee);
console.log(coffee.description()); // "Coffee, Milk, Sugar"
```

### 跨学科含义

- **在 TypeScript/Java 中**：语言原生支持的装饰器语法（`@decorator`）是装饰器模式的语法糖。Angular 大量使用装饰器（`@Component`、`@Injectable`）。
- **在前端开发中**：React 的高阶组件（HOC）是装饰器模式的应用——接收一个组件，返回增强后的新组件。中间件模式（如 Express/Koa 中间件、Redux middleware）也体现了类似思想。

### 知识网络

- **父级概念**：[[设计模式|MOC-设计模式]] — 装饰器模式是结构型设计模式之一
- **相关概念**：
  - [[组合模式]] — 两者都基于对象组合
  - [[适配器模式]] — 都是包装对象，适配器改变接口，装饰器增强接口