正面	反面	标签
单例模式是什么？在前端中有哪些应用场景？	<strong>定义</strong><br>确保一个类只有一个实例，并提供一个全局访问点。<br><br><strong>实现方式</strong><br>class Singleton {<br>  constructor() {<br>    if (!Singleton.instance) {<br>      Singleton.instance = this<br>    }<br>    return Singleton.instance<br>  }<br>}<br><br>// 闭包实现<br>const Singleton = (function() {<br>  let instance = null<br>  return function() {<br>    if (!instance) instance = this<br>    return instance<br>  }<br>})()<br><br><strong>应用场景</strong><br>• 全局状态管理（Vuex、Pinia）<br>• 全局弹窗组件（Toast、Modal）<br>• 日志记录器<br>• 数据库连接池	设计模式
观察者模式和发布订阅模式有什么区别？	<strong>观察者模式</strong><br>• 目标和观察者直接通信，互相知道对方<br>• 同步执行<br>• 紧耦合<br><br><strong>发布订阅模式</strong><br>• 通过事件中心调度，发布者和订阅者互不知晓<br>• 异步执行（通常）<br>• 松耦合<br><br><strong>代码示例对比</strong><br>// 观察者模式<br>class Subject {<br>  constructor() { this.observers = [] }<br>  addObserver(observer) { this.observers.push(observer) }<br>  notify(data) { this.observers.forEach(o => o.update(data)) }<br>}<br><br>// 发布订阅模式<br>class EventBus {<br>  constructor() { this.events = {} }<br>  on(name, fn) {<br>    if (!this.events[name]) this.events[name] = []<br>    this.events[name].push(fn)<br>  }<br>  emit(name, data) {<br>    if (!this.events[name]) return<br>    this.events[name].forEach(fn => fn(data))<br>  }<br>}<br><br><strong>前端应用</strong><br>• 观察者模式：Vue 响应式系统<br>• 发布订阅模式：eventBus、Node.js EventEmitter	设计模式
工厂模式是什么？在前端中有哪些应用？	<strong>定义</strong><br>定义一个创建对象的接口，由子类决定实例化哪个类。<br><br><strong>简单工厂</strong><br>class Button {<br>  constructor(text, type) {<br>    this.text = text<br>    this.type = type<br>  }<br>  render() {<br>    const btn = document.createElement('button')<br>    btn.textContent = this.text<br>    btn.className = `btn btn-${this.type}`<br>    return btn<br>  }<br>}<br><br>class ButtonFactory {<br>  static create(type, text) {<br>    switch(type) {<br>      case 'primary': return new Button(text, 'primary')<br>      case 'danger': return new Button(text, 'danger')<br>      default: return new Button(text, 'default')<br>    }<br>  }<br>}<br><br><strong>应用场景</strong><br>• 创建不同类型的组件（按钮、弹窗）<br>• Vue 的 createElement / h 函数<br>• 解析不同格式的数据（JSON、XML）	设计模式
策略模式是什么？请举例说明。	<strong>定义</strong><br>定义一系列算法，将每个算法封装起来，并使它们可以相互替换。<br><br><strong>代码示例</strong><br>// 表单验证策略<br>const strategies = {<br>  isNonEmpty: (value, errorMsg) => {<br>    if (value === '') return errorMsg<br>  },<br>  minLength: (value, length, errorMsg) => {<br>    if (value.length < length) return errorMsg<br>  },<br>  isMobile: (value, errorMsg) => {<br>    if (!/^1[3-9]\d{9}$/.test(value)) return errorMsg<br>  }<br>}<br><br>class Validator {<br>  constructor() { this.rules = [] }<br>  add(value, rule, errorMsg) {<br>    const [name, param] = rule.split(':')<br>    this.rules.push(() => strategies[name](value, param, errorMsg))<br>  }<br>  validate() {<br>    for (const rule of this.rules) {<br>      const msg = rule()<br>      if (msg) return msg<br>    }<br>  }<br>}<br><br><strong>应用场景</strong><br>• 表单验证<br>• 动画缓动函数<br>• 不同角色的权限计算	设计模式
装饰器模式是什么？在 JavaScript 中如何实现？	<strong>定义</strong><br>动态地给对象添加额外的职责，不改变原对象结构。<br><br><strong>代码示例</strong><br>// 高阶函数装饰器<br>function log(target, name, descriptor) {<br>  const original = descriptor.value<br>  descriptor.value = function(...args) {<br>    console.log(`调用 ${name}，参数：`, args)<br>    return original.apply(this, args)<br>  }<br>  return descriptor<br>}<br><br>class User {<br>  @log<br>  sayHello(name) {<br>    return `Hello, ${name}`<br>  }<br>}<br><br>// 函数装饰器<br>function debounce(fn, delay) {<br>  let timer = null<br>  return function(...args) {<br>    if (timer) clearTimeout(timer)<br>    timer = setTimeout(() => fn.apply(this, args), delay)<br>  }<br>}<br><br>const handleInput = debounce(() => {<br>  console.log('搜索')<br>}, 300)<br><br><strong>应用场景</strong><br>• 日志记录<br>• 性能监控<br>• 权限校验<br>• 防抖节流装饰器	设计模式
代理模式是什么？在 JavaScript 中有哪些应用？	<strong>定义</strong><br>为其他对象提供一种代理，控制对这个对象的访问。<br><br><strong>ES6 Proxy</strong><br>const handler = {<br>  get(target, key) {<br>    if (key === 'age') {<br>      console.log(`获取年龄：${target[key]}`)<br>    }<br>    return target[key]<br>  },<br>  set(target, key, value) {<br>    if (key === 'age' && value < 0) {<br>      throw new Error('年龄不能为负数')<br>    }<br>    target[key] = value<br>    return true<br>  }<br>}<br>const person = new Proxy({ name: '张三', age: 18 }, handler)<br><br><strong>应用场景</strong><br>• 数据校验（set 拦截）<br>• 访问控制（权限校验）<br>• 缓存代理（计算结果缓存）<br>• 虚拟代理（图片预加载）<br><br><strong>图片预加载示例</strong><br>const imgProxy = (function() {<br>  const img = new Image()<br>  const proxyImg = new Image()<br>  proxyImg.onload = () => img.src = proxyImg.src<br>  return { setSrc: (src) => { img.src = 'loading.gif'; proxyImg.src = src } }<br>})()	设计模式
发布订阅模式如何实现一个简易 eventBus？	<strong>代码实现</strong><br>class EventBus {<br>  constructor() {<br>    this.events = {}<br>  }<br><br>  on(name, callback) {<br>    if (!this.events[name]) {<br>      this.events[name] = []<br>    }<br>    this.events[name].push(callback)<br>    return this<br>  }<br><br>  once(name, callback) {<br>    const onceCallback = (...args) => {<br>      callback(...args)<br>      this.off(name, onceCallback)<br>    }<br>    return this.on(name, onceCallback)<br>  }<br><br>  emit(name, ...args) {<br>    if (!this.events[name]) return<br>    this.events[name].forEach(cb => cb(...args))<br>    return this<br>  }<br><br>  off(name, callback) {<br>    if (!this.events[name]) return<br>    if (!callback) {<br>      delete this.events[name]<br>      return<br>    }<br>    this.events[name] = this.events[name].filter(cb => cb !== callback)<br>    return this<br>  }<br><br>  clear(name) {<br>    if (name) {<br>      delete this.events[name]<br>    } else {<br>      this.events = {}<br>    }<br>    return this<br>  }<br>}<br><br><strong>使用示例</strong><br>const bus = new EventBus()<br>bus.on('test', (data) => console.log(data))<br>bus.emit('test', 'hello')	设计模式
适配器模式是什么？在前端中有哪些应用？	<strong>定义</strong><br>将一个类的接口转换成客户期望的另一个接口，使原本不兼容的类可以一起工作。<br><br><strong>代码示例</strong><br>// 旧接口<br>class OldApi {<br>  getUser() {<br>    return { userName: '张三', userAge: 18 }<br>  }<br>}<br><br>// 新接口期望的数据格式<br>class Adapter {<br>  constructor(oldApi) {<br>    this.oldApi = oldApi<br>  }<br>  getUserInfo() {<br>    const old = this.oldApi.getUser()<br>    return {<br>      name: old.userName,<br>      age: old.userAge<br>    }<br>  }<br>}<br><br><strong>应用场景</strong><br>• 兼容不同版本的 API<br>• 统一第三方库的调用方式<br>• Vue 的 computed 适配数据格式<br>• axios 适配不同环境（浏览器/Node.js）	设计模式
如何实现数组扁平化（flatten）？有哪些方法？	<strong>方法一：递归</strong><br>function flatten(arr) {<br>  let result = []<br>  for (let i = 0; i < arr.length; i++) {<br>    if (Array.isArray(arr[i])) {<br>      result = result.concat(flatten(arr[i]))<br>    } else {<br>      result.push(arr[i])<br>    }<br>  }<br>  return result<br>}<br><br><strong>方法二：reduce</strong><br>function flatten(arr) {<br>  return arr.reduce((prev, cur) => {<br>    return prev.concat(Array.isArray(cur) ? flatten(cur) : cur)<br>  }, [])<br>}<br><br><strong>方法三：flat 方法（ES10）</strong><br>arr.flat(Infinity)<br><br><strong>方法四：扩展运算符 + some</strong><br>while (arr.some(Array.isArray)) {<br>  arr = [].concat(...arr)<br>}<br>return arr	算法手写
如何实现数组去重？有哪些方法？	<strong>方法一：Set（推荐）</strong><br>const unique = arr => [...new Set(arr)]<br><br><strong>方法二：filter + indexOf</strong><br>const unique = arr => arr.filter((item, index) => arr.indexOf(item) === index)<br><br><strong>方法三：reduce + includes</strong><br>const unique = arr => arr.reduce((prev, cur) => {<br>  return prev.includes(cur) ? prev : [...prev, cur]<br>}, [])<br><br><strong>方法四：Map（对象去重）</strong><br>const unique = arr => {<br>  const map = new Map()<br>  return arr.filter(item => !map.has(item) && map.set(item, 1))<br>}<br><br><strong>对象数组去重（根据某个字段）</strong><br>const uniqueByKey = (arr, key) => {<br>  const map = new Map()<br>  return arr.filter(item => !map.has(item[key]) && map.set(item[key], 1))<br>}	算法手写
如何实现函数柯里化（curry）？	<strong>定义</strong><br>柯里化是将一个多参数函数转换为一系列单参数函数的过程。<br><br><strong>实现</strong><br>function curry(fn, ...args) {<br>  // 获取原函数的参数数量<br>  const length = fn.length<br>  return function(...newArgs) {<br>    const allArgs = [...args, ...newArgs]<br>    if (allArgs.length >= length) {<br>      return fn(...allArgs)<br>    } else {<br>      return curry(fn, ...allArgs)<br>    }<br>  }<br>}<br><br><strong>使用示例</strong><br>const add = (a, b, c) => a + b + c<br>const curriedAdd = curry(add)<br>curriedAdd(1)(2)(3)  // 6<br>curriedAdd(1, 2)(3)  // 6<br>curriedAdd(1)(2, 3)  // 6<br><br><strong>进阶：支持占位符</strong><br>function curryWithPlaceholder(fn) {<br>  // 实现略，可使用 Symbol 作为占位符<br>}	算法手写
如何实现组合函数（compose）？	<strong>定义</strong><br>将多个函数从右向左组合执行，前一个函数的输出作为后一个函数的输入。<br><br><strong>实现一：reduceRight</strong><br>function compose(...fns) {<br>  if (fns.length === 0) return args => args<br>  if (fns.length === 1) return fns[0]<br>  return fns.reduceRight((prev, cur) => (...args) => cur(prev(...args)))<br>}<br><br><strong>实现二：reduce（反向遍历）</strong><br>function compose(...fns) {<br>  return (...args) => {<br>    let result = fns[fns.length - 1](...args)<br>    for (let i = fns.length - 2; i >= 0; i--) {<br>      result = fns[i](result)<br>    }<br>    return result<br>  }<br>}<br><br><strong>使用示例</strong><br>const add1 = x => x + 1<br>const multiply2 = x => x * 2<br>const toString = x => String(x)<br>const fn = compose(toString, multiply2, add1)<br>fn(5)  // "12"（5+1=6，6*2=12，转字符串"12"）<br><br><strong>pipe（从左向右组合）</strong><br>function pipe(...fns) {<br>  return fns.reduce((prev, cur) => (...args) => cur(prev(...args)))<br>}	算法手写
如何实现 LRU 缓存机制？	<strong>定义</strong><br>LRU（Least Recently Used）最近最少使用缓存，当缓存满时，淘汰最久未使用的数据。<br><br><strong>实现（Map + 双向链表）</strong><br>class LRUCache {<br>  constructor(capacity) {<br>    this.capacity = capacity<br>    this.cache = new Map()  // Map 保持插入顺序<br>  }<br><br>  get(key) {<br>    if (!this.cache.has(key)) return -1<br>    const value = this.cache.get(key)<br>    // 刷新到最新（删除再插入）<br>    this.cache.delete(key)<br>    this.cache.set(key, value)<br>    return value<br>  }<br><br>  put(key, value) {<br>    if (this.cache.has(key)) {<br>      this.cache.delete(key)<br>    } else if (this.cache.size >= this.capacity) {<br>      // 淘汰最久未使用（Map 中第一个 key）<br>      const firstKey = this.cache.keys().next().value<br>      this.cache.delete(firstKey)<br>    }<br>    this.cache.set(key, value)<br>  }<br>}<br><br><strong>复杂度</strong><br>• 时间复杂度 O(1)<br>• 空间复杂度 O(n)<br><br><strong>注意事项</strong><br>• 使用 Map 保持插入顺序，适合 LRU 实现<br>• 面试中也可能要求手写双向链表版本	算法手写
如何实现深拷贝（deepClone）完整版？	<strong>完整版实现</strong><br>function deepClone(obj, map = new WeakMap()) {<br>  // 处理 null 和基本类型<br>  if (obj === null || typeof obj !== 'object') return obj<br>  <br>  // 处理循环引用<br>  if (map.has(obj)) return map.get(obj)<br>  <br>  // 处理日期对象<br>  if (obj instanceof Date) return new Date(obj)<br>  <br>  // 处理正则对象<br>  if (obj instanceof RegExp) return new RegExp(obj)<br>  <br>  // 处理数组和对象<br>  const clone = Array.isArray(obj) ? [] : {}<br>  map.set(obj, clone)<br>  <br>  // 拷贝 Symbol 属性<br>  const symbols = Object.getOwnPropertySymbols(obj)<br>  symbols.forEach(sym => {<br>    clone[sym] = deepClone(obj[sym], map)<br>  })<br>  <br>  // 拷贝普通属性<br>  for (let key in obj) {<br>    if (obj.hasOwnProperty(key)) {<br>      clone[key] = deepClone(obj[key], map)<br>    }<br>  }<br>  <br>  return clone<br>}<br><br><strong>注意事项</strong><br>• 使用 WeakMap 解决循环引用<br>• 特殊处理 Date、RegExp<br>• 处理 Symbol 属性<br>• 只拷贝自有属性	算法手写
如何实现 Promise.all？	<strong>定义</strong><br>接收 Promise 数组，所有成功则返回结果数组，任一失败则返回失败结果。<br><br><strong>实现</strong><br>function promiseAll(promises) {<br>  return new Promise((resolve, reject) => {<br>    if (!Array.isArray(promises)) {<br>      return reject(new TypeError('Argument must be an array'))<br>    }<br>    const results = []<br>    let completed = 0<br>    const total = promises.length<br>    <br>    if (total === 0) return resolve(results)<br>    <br>    promises.forEach((promise, index) => {<br>      Promise.resolve(promise).then(value => {<br>        results[index] = value<br>        completed++<br>        if (completed === total) resolve(results)<br>      }).catch(reject)<br>    })<br>  })<br>}<br><br><strong>关键点</strong><br>• 使用 Promise.resolve 包装非 Promise 值<br>• 保持结果顺序与输入一致<br>• 计数判断是否全部完成	算法手写
如何实现 Promise.race？	<strong>定义</strong><br>接收 Promise 数组，返回最先完成（成功或失败）的结果。<br><br><strong>实现</strong><br>function promiseRace(promises) {<br>  return new Promise((resolve, reject) => {<br>    if (!Array.isArray(promises)) {<br>      return reject(new TypeError('Argument must be an array'))<br>    }<br>    promises.forEach(promise => {<br>      Promise.resolve(promise).then(resolve).catch(reject)<br>    })<br>  })<br>}<br><br><strong>使用示例</strong><br>const p1 = new Promise(resolve => setTimeout(() => resolve('慢'), 1000))<br>const p2 = new Promise(resolve => setTimeout(() => resolve('快'), 500))<br>Promise.race([p1, p2]).then(result => {<br>  console.log(result)  // '快'<br>})<br><br><strong>关键点</strong><br>• 谁先完成就返回谁<br>• 不取消其他 Promise 的执行<br>• 可用于超时控制	算法手写