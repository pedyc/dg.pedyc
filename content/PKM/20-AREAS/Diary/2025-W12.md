---
topic: [个人成长/周报]
uid: '<% tp.file.creation_date("YYYYMMDDHHmm") %>'
title: 2025-W12
tags: [个人成长/weekly]
date-created: 2025-03-23
date-modified: 2025-03-24
---

<%*
let 一言 = ""
let 来源 = ""
let 作者 = ""

await fetch('https://v1.hitokoto.cn/?c=d&c=h&c=i&c=j')
.then(response => response.json())
.then(data => {
		一言 = data.hitokoto
		来源 = data.from
		作者 = data.from_who === null? ' 佚名 ': data.from_who
})
-%>

> [!quote] 本周箴言
> <% 一言 %> —— 《<% 来源 %>》 · <% 作者 %>

## 📋 任务全景（本周任务追踪）

```dataviewjs
const startDate = tp.date.now("YYYY-MM-DD", -6)
const endDate = tp.date.now("YYYY-MM-DD")

dv.taskList(dv.pages().file.tasks
  .where(t => {
    const created = t.created?.toISODate?.() || t.created
    return created >= startDate && created <= endDate
  })
  .groupBy(t => t.status)
)
```

## 🌟 周焦点（本周核心目标）

- [ ] 核心目标 1
- [ ] 核心目标 2

## 📈 周维度（进展追踪）

```dataview
TABLE WITHOUT ID
  file.link AS "日期",
  选择(
    any(contains(状态, "LiSmilePlus")), "😄", 
    any(contains(状态, "LiSmile")), "🙂",
    any(contains(状态, "LiMeh")), "😐",
    "❓") AS "状态趋势"
WHERE contains(tags, "个人成长/diary") 
AND file.day >= date(<% tp.date.now("YYYY-MM-DD", -6) %>)
AND file.day <= date(<% tp.date.now("YYYY-MM-DD") %>)
SORT file.day ASC
```

> [!NOTE] 周度战略复盘
> 本周整体评价：⭐️⭐️⭐️⭐️
> **任务完成率**：<% await tp.user.calculate_task_completion(tp, 7) %>%
> **核心洞察**：
> - TODO

## 🌌 星云集（本周碎片记录）

```dataview
LIST L.text
FROM "个人成长/diary"
WHERE file.day >= date(<% tp.date.now("YYYY-MM-DD", -6) %>)
AND file.day <= date(<% tp.date.now("YYYY-MM-DD") %>)
FLATTEN file.lists AS L
WHERE contains(L.tags, "#闪念")
```

## 📆 周历全景

```dataview
CALENDAR file.day
FROM "个人成长/diary"
WHERE file.day >= date(<% tp.date.now("YYYY-MM-DD", -6) %>)
AND file.day <= date(<% tp.date.now("YYYY-MM-DD") %>)
```
