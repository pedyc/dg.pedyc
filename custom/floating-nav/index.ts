/**
 * FloatingNav — component-only plugin.
 *
 * The component is registered via the `components` subpath declared in the
 * package.json `quartz` manifest. Options from quartz.config.yaml are passed
 * to the component constructor by the layout resolver.
 */
export {}
